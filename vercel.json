{
  "functions": {
    "api/**/*.js": { "includeFiles": "server/schema.sql", "maxDuration": 60 }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/[...path].js" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
